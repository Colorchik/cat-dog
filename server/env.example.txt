# Server Environment Variables Example
# Скопируйте этот файл в .env и заполните значения

# Server Port (Railway автоматически устанавливает PORT)
PORT=6001

# Database URL (Railway предоставляет DATABASE_URL)
DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# Node Environment
NODE_ENV=production

# Frontend URL (для CORS в production)
# В Railway это будет URL вашего деплоя, например: https://your-app.railway.app
FRONTEND_URL=

